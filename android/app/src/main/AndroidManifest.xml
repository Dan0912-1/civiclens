<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: route https://capitolkey.vercel.app/bill/*, etc. to this app.
                 autoVerify=true tells Android to hit /.well-known/assetlinks.json on
                 first install and, if the signing-key SHA-256 matches, open these
                 URLs in CapitolKey instead of the browser picker. If the assetlinks
                 file isn't published yet, the filter still works — Android just
                 shows the "open with" chooser like a normal VIEW intent. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="capitolkey.vercel.app" />
                <data android:host="capitolkey.org" />
                <data android:host="www.capitolkey.org" />
                <data android:pathPrefix="/bill/" />
                <data android:pathPrefix="/bookmarks" />
                <data android:pathPrefix="/results" />
                <data android:pathPrefix="/search" />
                <data android:pathPrefix="/classroom" />
            </intent-filter>

            <!-- Custom scheme for Supabase OAuth callback — kept separate from
                 the https App Links filter so it isn't part of autoVerify. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.danieljacius.capitolkey" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
