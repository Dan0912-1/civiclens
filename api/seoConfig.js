// Single source of truth for the canonical public origin, shared by the SEO
// modules (sitemap + dynamic bill renderer) so the domain is defined once.
export const SITE_URL = 'https://capitolkey.org'
